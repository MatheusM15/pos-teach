name: CI/CD

on:
  push:
    branches:
      - main # Mude para o nome da sua branch principal, se necessário
  pull_request:
    branches:
      - main # Mude para o nome da sua branch principal, se necessário

jobs:
  compliance:
    uses: MatheusM15/pos-teach/.github/workflows/compliance.yml@main
